PORT=3000
NODE_ENV=development

# Person 1 (Auth) fills these in when implementing mock JWT
JWT_SECRET=change_me_in_your_own_.env
JWT_EXPIRES_IN=1h

# Person 4 (Cache) can tune this
CACHE_TTL_MS=60000
