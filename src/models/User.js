/**
 * Shape reference only (not enforced at runtime here) — Person 1 owns this.
 * {
 *   id: string,
 *   name: string,
 *   email: string,
 *   passwordHash: string,
 *   preferences: object,
 *   createdAt: string (ISO date)
 * }
 */
module.exports = {};
