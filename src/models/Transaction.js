/**
 * Shape reference only (not enforced at runtime here) — Shiva owns this.
 * {
 *   id: string,
 *   userId: string,
 *   type: 'income' | 'expense',
 *   category: string,
 *   amount: number,
 *   date: string (ISO date),
 *   createdAt: string (ISO date)
 * }
 */
module.exports = {};
